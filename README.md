# MyMoney

Application to control and check spendings.

## Prerequisites

* Apache Maven
* Node.js

## Setup

* Run `mvn install`;
* Run `npm install`.

## Unit testing

To execute all unit tests, run `npm test`.
